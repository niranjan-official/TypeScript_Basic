/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['cdn.weatherapi.com'], // Add the domain you want to optimize images for
    },
    nextConfig
  };
